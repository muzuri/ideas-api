type Idea{
    id: ID!
    updated: String!
    created: String!
    idea: String!
    description: String!
    author: User
    upvotes: Int
    comment:[Comment!]

    
}
type Query{
    ideas(page: Int): [Idea!]
}